package com.appforcross.editor.palette

/**
 * Единый источник правды для порогов каталожного маппинга.
 *
 * Используется и прод-кодом, и тестами.
 */
object CatalogThresholds {
    const val AVG: Float = 1.0f
    const val MAX: Float = 5.0f
}
